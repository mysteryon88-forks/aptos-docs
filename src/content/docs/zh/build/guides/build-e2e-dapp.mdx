---
title: "在 Aptos 上构建端到端 Dapp"
description: "构建待办事项列表 dapp 的完整教程，从智能合约到前端，并在 Aptos 上集成钱包。"
sidebar:
  label: "构建端到端 Dapp"
---

学习新框架或编程语言的常见方式是构建一个简单的待办事项列表。在本教程中，我们将学习如何构建端到端的待办事项列表 dapp，从智能合约端到前端，最后使用钱包与两者进行交互。

请参阅 [源代码](https://github.com/aptos-labs/aptos-core/tree/main/aptos-move/move-examples/my_first_dapp) 中的完整代码。

## 章节

在满足下面描述的[前提条件](#prerequisites)和[设置](#setup)后，您将按以下顺序学习本教程：

1. [创建智能合约](/zh/build/guides/build-e2e-dapp/1-create-smart-contract)
2. [设置前端](/zh/build/guides/build-e2e-dapp/2-set-up-the-frontend)
3. [从链上获取数据](/zh/build/guides/build-e2e-dapp/3-fetch-data-from-chain)
4. [向链上提交数据](/zh/build/guides/build-e2e-dapp/4-submit-data-to-chain)
5. [处理任务](/zh/build/guides/build-e2e-dapp/5-handle-tasks)

## 前提条件

您必须具备：

- [node 和 npm](https://nodejs.org/en/)

虽然我们会解释一些 React 的决策，但我们不会深入探讨 React 的工作原理；因此我们假设您有一些 React 的使用经验。

## 设置

在本节中，我们将创建一个 `my-first-dapp` 目录来保存我们的项目文件，包括客户端代码（基于 React）和 Move 代码（我们的智能合约）。

为此，我们将使用 [create-aptos-dapp](/zh/build/create-aptos-dapp) 来创建项目。

1. 打开终端并导航到项目的所需目录（例如，`Desktop` 目录）。
2. 运行 `npx create-aptos-dapp@latest` 来创建项目。

```shellscript filename="Terminal"
npx create-aptos-dapp@latest
```

3. 按照说明创建项目。

- 为项目选择一个名称，例如 `my-first-dapp`。
- 选择 `Full-stack project` 选项。
- 选择 `Boilerplate Template` 选项。
- 为简单起见，选择不使用 Surf。
- 选择 `Vite app` 框架选项。
- 选择 `Devnet` 网络选项。

该工具将在与项目同名的目录中创建项目并安装所需的依赖项。

按照 `Next Steps` 说明操作。

现在让我们[创建智能合约](/zh/build/guides/build-e2e-dapp/1-create-smart-contract)。
